name: "Setup Poetry Environment"
description: "Set up Python and Poetry with dependency caching"

inputs:
  python-version:
    description: "Python version to use"
    default: "3.13"
    required: false
  poetry-version:
    description: "Poetry version to install"
    default: "2.3.2"
    required: false
  poetry-install-args:
    description: "Additional arguments for poetry install (e.g. --with dev --with test)"
    default: ""
    required: false

runs:
  using: "composite"
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: ${{ inputs.poetry-version }}
        virtualenvs-create: true
        virtualenvs-in-project: true
        installer-parallel: true

    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v5
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ inputs.python-version }}-${{ inputs.poetry-version }}-${{ inputs.poetry-install-args }}-${{ hashFiles('**/poetry.lock') }}
        restore-keys: |
          venv-${{ runner.os }}-${{ inputs.python-version }}-${{ inputs.poetry-version }}-${{ inputs.poetry-install-args }}-
          venv-${{ runner.os }}-${{ inputs.python-version }}-${{ inputs.poetry-version }}-

    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      shell: bash
      run: poetry install --no-interaction --no-root ${{ inputs.poetry-install-args }}

    - name: Install project
      shell: bash
      run: poetry install --no-interaction --only-root
